#include "Node.h"

void Node::translate(float x, float y, float z)
{
	/*
	glm::mat4 translateMat = glm::translate(glm::mat4(1.0f), glm::vec3(x, y, z));
	transformMatrix = transformMatrix;
	*/
}

void Node::rotate(int axis, float angle)
{
	/*
	glm::mat4 rotatMat = glm::rotate(,);
	transMat = translate * rotatation;
	*/
}
